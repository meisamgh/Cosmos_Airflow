# .env
DB_HOST=localhost
DB_PORT=5432
DB_USER=pipeline_user
DB_PASSWORD=pipeline_password
DB_NAME=pipeline_db

# Directory where your CSV (and other data) is stored
DATA_DIR=/opt/airflow/data

# CSV file name
CSV_FILE=sample_data.csv

# Batch size for processing CSV records
BATCH_SIZE=100
